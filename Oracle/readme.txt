npm install -g // installs the index.js into the node_modules folder (linux is /usr/lib/node_modules i think)

node index.js // just runs the script


Check the package.json for dependencies

to install new dependencies use npm install <package> -save
